# Rust build artifacts
/target/
/build_artifacts/
**/*.rs.bk
*.pdb

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# NOTE: Cargo.lock is intentionally NOT ignored. This workspace builds
# binaries (services), so the lockfile must be committed for reproducible
# builds. (It was previously ignored while also being tracked — resolved
# in favour of tracking.)

# macOS / NFS artifacts
._*
.nfs*

# Secret material — never commit
*.agekey
*.age
age.key
keys.txt
*.dec.yaml
*.plain.yaml
.git-commit-msg.tmp
